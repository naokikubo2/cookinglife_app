<%- if controller_name != 'sessions' %>
  <div class="float-right">
   既に会員登録がお済みの方⇨ <%= link_to "ログイン", new_session_path(resource_name), class: 'btn btn-success' %><br />
  </div>
<% end %>

<%- if devise_mapping.registerable? && controller_name != 'registrations' %>
  <div class="float-right">
    会員登録がお済みで無い方⇨ <%= link_to "新規会員登録", new_registration_path(resource_name), class: 'btn btn-success' %><br />
  </div>
<% end %>
